{
  "name": "petstore-api-tests",
  "version": "2.0.0",
  "description": "Playwright tests for PetStore API with Page Object Model and Allure Reports",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:allure": "playwright test && allure generate allure-results --clean -o allure-report",
    "allure:report": "allure generate allure-results --clean -o allure-report && allure open allure-report",
    "clean:reports": "rimraf playwright-report allure-report allure-results test-results"
  },
  "keywords": ["playwright", "api-testing", "petstore", "page-object-model", "allure"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "allure-playwright": "^2.10.0",
    "allure-commandline": "^2.25.0",
    "rimraf": "^5.0.5"
  }
}
